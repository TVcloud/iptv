name: 'REFRESH BOT'

on:
  workflow_dispatch:
  push:
    paths: 
      - '*.xml'
env:
  ACTIONS_ALLOW_UNSECURE_COMMANDS: true
jobs:
  refreshbot:
    name: Python PLAYLIST_BOT
    runs-on: ubuntu-latest
    steps:
      - name: Refresh CDN
        run: |
          sleep 300
          curl https://purge.jsdelivr.net/gh/tvcloud/iptv/epg.xml
